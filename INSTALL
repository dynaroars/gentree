1. Start container:
    ```
    docker run -it --rm --tmpfs /mnt/ramdisk unsatx/gentree_docker:icse21 bash
    ```

2. Run GenTree (inside container):
    ```
    cd ~/gentree/wd

    # Motivation example
    ./gt -J2 -cx -BF @ex_paper        # figure 1b
    ./gt -J2 -cx -BF @ex_paper --full # figure 1b (all configurations / groundtruth)

    # Benchmark programs
    ./gt -J2 -cx -GF 2/id             # coreutils `id` (C)
    ./gt -J2 -cx -YF 2/vsftpd         # vsftpd (Otter)
    ./gt -J2 -cx -GF 2/grin -j 8      # grin (Python), using 8 cores; maybe slow.
    ```

Note: if you get permission error on directory /mnt/ramdisk while running GenTree, run the following command in the container: `sudo chmod 777 -R /mnt/ramdisk`


If GenTree is working correctly, when exiting, it will print the locations and interactions found by the algoritm:
...
======
# M/H: 144 / 42
# Last rebuild:   iter 3  num_configs 47
L7, 
-
(and (= s |0|) (= e |2|) (or (= u |0|) (= v |0|)))
-
1 0 MM 4 H 3 HMM
======
...
